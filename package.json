{
  "name": "stryd-profile",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "jshint": "^2.9.5",
    "jshint-stylish": "^2.2.1",
    "morgan": "^1.9.0",
    "polyserve": "^0.23.0",
    "route-map": "github:tony19/route-map#0.0.1"
  },
  "scripts": {
    "build": "polymer build",
    "lint": "jshint --reporter=node_modules/jshint-stylish **/*.js && polymer lint --input app/elements/**/*.html",
    "serve": "node server",
    "start": "npm run serve"
  },
  "engines": {
    "node": ">=7.6"
  }
}
